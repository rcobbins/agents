# Server Configuration
PORT=3001
HOST=localhost

# Client URL for CORS
CLIENT_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Claude CLI Path (defaults to $HOME/bin/claude)
CLAUDE_PATH=

# Framework Directory (defaults to $HOME/agent-framework)
FRAMEWORK_DIR=